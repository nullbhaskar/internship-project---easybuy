/**
 * Barrel export for all custom hooks.
 * Import from 'hooks' directly instead of individual files.
 *
 * Example:
 *   import { useDebounce, useAdminCheck } from '../hooks';
 */

export { useAdminCheck } from './useAdminCheck';
export { useDebounce } from './useDebounce';
export { useThemePreference } from './useThemePreference';
